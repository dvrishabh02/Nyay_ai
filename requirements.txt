# ── Data Ingestion & Processing ──
pandas>=2.1
pyarrow>=15.0         # Parquet file support
tiktoken>=0.6         # Token counting for chunk validation

# ── Embeddings & Vector Search ──
sentence-transformers>=2.3
torch>=2.1
faiss-cpu>=1.7        # In-memory vector index
rank-bm25>=0.2.2      # BM25 sparse keyword index
numpy>=1.26

# ── LLM (query time only) ──
openai>=1.0           # Groq generation via OpenAI-compatible API
anthropic>=0.18       # Claude API (unused; Groq is the active provider)

# ── Backend ──
fastapi>=0.109
uvicorn>=0.27
pydantic>=2.5

# ── Database ──
supabase>=2.3

# ── Utilities ──
python-dotenv>=1.0
tqdm>=4.66
loguru>=0.7

# ── Deferred (uncomment when needed) ──
# scrapy>=2.11          # Web scraping (Week 5)
# playwright>=1.40      # Browser automation for scraping
# PyMuPDF>=1.23         # PDF parsing (when adding Kaggle PDFs)
# pytesseract>=0.3.10   # OCR for scanned PDFs
# datasets>=2.16        # HuggingFace datasets API
# kaggle>=1.6           # Kaggle API
