# Base URL of the FastAPI backend. Read server-side only (app/api/chat/route.ts) —
# never exposed to the browser bundle. Defaults to http://localhost:8000 if unset.
API_BASE_URL=http://localhost:8000
